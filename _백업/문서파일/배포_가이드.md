# iParking-Bot 배포 가이드

이 가이드는 다른 컴퓨터에서 iParking-Bot을 실행하기 위한 배포 방법을 설명합니다.

## 📦 배포 패키지 구성

다음 파일들을 압축하여 배포하세요:

### 필수 파일
```
iparking-bot/
├── bot_Simple_logic.py          # 메인 봇 프로그램
├── dashboard.py                  # 대시보드 (웹)
├── firebase_helper.py            # Firebase 연동
├── firebase-credentials.json     # Firebase 인증 파일 (필수)
└── requirements.txt              # Python 패키지 목록

설치_및_실행.bat                  # 자동 설치 스크립트
배포_가이드.md                    # 이 파일
```

### 선택 파일 (있으면 포함)
```
주차_DB_파일/                     # CSV 파일 저장 폴더
  └── *.csv                       # 주차 DB 파일들
```

## 🚀 설치 및 실행 방법

### 방법 1: 자동 설치 (권장)

1. **압축 파일 압축 해제**
   - 원하는 위치에 압축 해제 (예: `C:\iparking-bot`)

2. **자동 설치 스크립트 실행**
   - `설치_및_실행.bat` 파일을 더블클릭
   - 자동으로 Python 확인, 가상환경 생성, 패키지 설치가 진행됩니다

3. **프로그램 실행**
   - 설치 완료 후 메뉴에서 선택:
     - `1`: 주차권 등록 봇 실행
     - `2`: 대시보드 실행 (웹 브라우저)

### 방법 2: 수동 설치

1. **Python 설치 확인**
   ```bash
   python --version
   ```
   - Python 3.8 이상 필요

2. **가상환경 생성**
   ```bash
   python -m venv venv
   ```

3. **가상환경 활성화**
   ```bash
   venv\Scripts\activate
   ```

4. **패키지 설치**
   ```bash
   pip install -r requirements.txt
   ```

5. **프로그램 실행**
   ```bash
   # 봇 실행
   cd iparking-bot
   python bot_Simple_logic.py
   
   # 대시보드 실행
   cd iparking-bot
   streamlit run dashboard.py
   ```

## ⚙️ 설정 파일

### Firebase 인증 파일
- `iparking-bot/firebase-credentials.json` 파일이 필요합니다
- 이 파일이 없으면 Firebase 기능을 사용할 수 없습니다
- Firebase 콘솔에서 다운로드한 인증 파일을 이 위치에 배치하세요

### CSV 파일 경로
- 프로그램은 자동으로 `주차_DB_파일` 폴더를 생성합니다
- CSV 파일은 이 폴더에 저장하면 자동으로 인식됩니다

## 📝 주의사항

1. **경로 문제 해결됨**
   - 모든 경로가 상대 경로로 변경되어 어느 위치에 설치해도 작동합니다
   - 절대 경로를 사용하지 않으므로 다른 컴퓨터에서도 바로 작동합니다

2. **Chrome 브라우저 필요**
   - Selenium을 사용하므로 Chrome 브라우저가 설치되어 있어야 합니다
   - Chrome이 설치되어 있지 않으면 자동으로 다운로드됩니다 (첫 실행 시)

3. **인터넷 연결 필요**
   - 패키지 설치 시 인터넷 연결이 필요합니다
   - 실행 중에도 인터넷 연결이 필요합니다 (iParking 사이트 접속)

## 🔄 업데이트 방법

프로그램을 업데이트할 때:

1. **새 파일로 교체**
   - 업데이트된 `.py` 파일들을 기존 파일과 교체

2. **패키지 업데이트 (필요시)**
   ```bash
   venv\Scripts\activate
   pip install -r requirements.txt --upgrade
   ```

3. **기존 데이터 유지**
   - `주차_DB_파일` 폴더와 `file_execution_log.json` 파일은 유지
   - 이 파일들은 실행 기록과 데이터를 저장합니다

## ❓ 문제 해결

### Python을 찾을 수 없음
- Python이 설치되어 있는지 확인
- 환경 변수 PATH에 Python이 추가되어 있는지 확인

### 패키지 설치 실패
- 인터넷 연결 확인
- 방화벽 설정 확인
- 수동 설치 시도:
  ```bash
  venv\Scripts\activate
  pip install selenium streamlit pandas firebase-admin streamlit-autorefresh
  ```

### Chrome 관련 오류
- Chrome 브라우저가 설치되어 있는지 확인
- ChromeDriver가 자동으로 다운로드되지 않으면 수동 설치 필요

### Firebase 연결 실패
- `firebase-credentials.json` 파일이 올바른 위치에 있는지 확인
- 파일 내용이 올바른지 확인

## 📞 지원

문제가 발생하면 다음을 확인하세요:
1. Python 버전 (3.8 이상)
2. 모든 필수 파일이 포함되어 있는지
3. `firebase-credentials.json` 파일 존재 여부
4. 인터넷 연결 상태

